<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

	<!-- persistenceUnitManager : -->
	<bean id="pum" class="org.springframework.orm.jpa.persistenceunit.DefaultPersistenceUnitManager">
 		<property name="defaultDataSource" ref="#{prop['baseDatasource']}"/>
 		<property name="defaultPersistenceUnitName" value="#{prop['orm.jpa.defaultPersistenceUnitName']}"/>
 		<property name="defaultPersistenceUnitRootLocation" value="classpath:#{prop['basePackage'].replace('.','/')}/"/>
 		<property name="packagesToScan" value="#{prop['basePackage']}"/>
 	</bean>

	<!-- entityManager : -->
  	<bean id="em" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitManager" ref="pum" />
		<property name="jpaDialect">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaDialect" />
		</property>
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
		</property>
		<property name="jpaPropertyMap">
			<map>
				<entry key="hibernate.cache.provider_class" value="org.hibernate.cache.HashtableCacheProvider"/>
				<entry key="hibernate.dialect" value="#{prop['orm.jpa.hibernate.dialect']}"/>
				<entry key="hibernate.show_sql" value="#{prop['orm.jpa.hibernate.show_sql']}" />
				<entry key="hibernate.format_sql" value="#{prop['orm.jpa.hibernate.format_sql']}" />
				<entry key="hibernate.hbm2ddl.auto" value="#{prop['orm.jpa.hibernate.hbm2ddl.auto']}" />
			</map>
		</property>
	</bean>
	
	<!-- 'transactionManager' is default value of @Transactional -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
	    <property name="entityManagerFactory" ref="em"/>
	</bean>
	<tx:annotation-driven transaction-manager="transactionManager"/>
 
</beans>
